KoNLPy (코엔엘파이): 한국어 NLP의 시작점과 같은 라이브러리입니다. 내부적으로 아래와 같은 다양한 오픈소스 분석기를 포함하고 있습니다.Okt (Open Korean Text): 분석 속도가 빠르고 오탈자 보정 기능이 있어 SNS 등 정제되지 않은 텍스트에 좋습니다.Kkma (꼬꼬마): 분석이 매우 정확하고 정교하지만 속도가 느린 편입니다.Mecab (메캅): 연산 속도가 압도적으로 빨라 대용량 데이터를 처리할 때 사실상 표준으로 쓰입니다.kiwipiepy (Kiwi): 최근 한국어 성능이 매우 뛰어나 많은 주목을 받고 있는 형태소


목표 : 3B 기준
rtx 6000 ada 기준으로 긴 시간을 gpu 점유를 못하기에 성능과 비용을 동시에 고려할 필요가 있음. 


한국어 데이터셋이 대부분

Byte-level한국어 텍스트 전처리(형태소 분석 등)를 미리 거치지 않고, 대규모 날것(Raw)의 한국어 코퍼스를 그대로 넣어서 학습시키는 Byte-level BPE 방식이 가장 성능이 좋습니다.공백과 조사가 붙은 형태를 모델이 통째로 학습하여, 추후 한국어 생성 속도가 빨라지고 생성 품질이 자연스러워집니다.


고민 Hugging Face tokenizers(속도 면에서 우위임)
SentencePiece


알게된 것 : 토크나이징은 model과 voca를 생성 > 이후 그걸 통해 토큰화를 하면 됨.